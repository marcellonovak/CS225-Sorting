#include <iostream>
#include <string>
#include "SortSelection.h"
using namespace std;

int main() {
	int arr[7] = { 7, 3, 20, 4, 4, 0, 9 };
	int n = sizeof(arr);

	//selectionSort(arr, n);
	return 0;
}